# Host toolkit fixture

`toolkit.js` is a snapshot of the pure Covel 0.0.39 tool wrapper, short-ID and result accessors (MIT, Covel Contributors). It validates the injected toolkit contract in standalone plugin tests. It is not installed with any plugin and does not replace testing against a real Covel host.
